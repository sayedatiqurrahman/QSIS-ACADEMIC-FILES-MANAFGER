[build]
  publish = "_site"
  command = "mkdir -p _site && cp index.html cached.html history.html _site/ && cp -r css js assets _site/ && cp manifest.json sw.js _site/"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  conditions = {Path = ["!/**"]}
